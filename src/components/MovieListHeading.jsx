import React from "react";

export default function MovieListHeading({ heading }) {
    return (
        <>
            <div className="col">
                <h1>{heading}</h1>
            </div>
        </>
    );
}
